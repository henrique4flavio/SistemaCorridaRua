package com.projeto.corrida.repository;

import com.projeto.corrida.model.LocalRetirada;
import org.springframework.data.repository.CrudRepository;

public interface LocalRetiradaRepository extends CrudRepository<LocalRetirada, Long> {
}
